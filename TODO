LEGEND:
    + Add
    - Remove
    * Fix
    ? Check
    ! High priority

JOB QUEUE:
+!  Handle service subscriptions
-   Avoid recursion, use lists as stacks instead
+   Advanced logger for ChibiOS (side project)
+   Optional automatic translation of &lt; and &amp; in XMLRPC strings
+   Support for BASE64 encoding for XMLRPC parameters
+   Support for struct/map for XMLRPC parameters
+   Gather bus stats/info
