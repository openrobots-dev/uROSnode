LEGEND:
    + Add
    - Remove
    * Fix
    ? Check
    ! High priority

JOB QUEUE:
*!  Service & constants banners
*!  Rename inmsgp/outmsgp to requestp/responsep in generated service calls
?!  Client service calls
+   Support for regex in urosstan.py entry point names
+   Support for named dependencies in urosstan.py entry point size
+   Advanced logger for ChibiOS (side project)
+   Support for BASE64 encoding for XMLRPC parameters
+   Support for struct/map for XMLRPC parameters
+   Gather bus stats/info
